server {
        listen 80;
        listen [::]:80;

        server_name this_domain www.this_domain;

        root /var/www/html;

        location ~ /.well-known/acme-challenge {
                allow all;
                root /var/www/html;
        }
}
